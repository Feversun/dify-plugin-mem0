chore: update ignore files, remove obsolete docs, and optimize code

Repository cleanup:
- Added `logs/` directory to both `.difyignore` and `.gitignore` to prevent log files from being tracked
- Removed outdated documentation files: `BUGFIX.md`, `GITHUB_SETUP.md`, `PROJECT_SUMMARY.md`, `QUICK_FIX.md`, `SINGLETON_ANALYSIS.md`, and `prepare_github.sh` to streamline the repository
- Updated `build_package.sh` to include the `utils` directory in the packaging process

Code optimizations:
- Fixed indentation errors in multiple tool files (`get_memory.py`, `add_memory.py`, etc.)
- Implemented separate semaphore for search operations in `AsyncLocalClient` to prevent search starvation by write-heavy workloads (add/update/delete operations)
- Added performance monitoring logs:
  * Semaphore wait time tracking with warning threshold (100ms) for abnormal queuing
  * Background event loop pending tasks monitoring with 80% threshold warning
  * All monitoring logs are triggered on each async memory operation for better observability
- Optimized logging logic:
  * Created `_log_semaphore_wait()` helper function to centralize semaphore wait time logging
  * Created `_warn_loop_pressure()` helper function to check and warn on high pending task counts
  * Removed redundant `loop = asyncio.get_running_loop()` assignments across all async methods
- Moved `SEMAPHORE_WAITING_THRESHOLD` constant to `utils/constants.py` for better maintainability
- Improved code quality by removing duplicate code and following Python best practices

